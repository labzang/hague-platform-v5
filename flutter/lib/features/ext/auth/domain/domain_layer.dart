/// Domain layer scaffold — mirrors backend `labzang.apps.ext.auth`.
/// Add entities, value objects, repository ports, use cases here.
abstract final class ExtAuthDomainLayer {
  const ExtAuthDomainLayer._();
}
