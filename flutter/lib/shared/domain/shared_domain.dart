/// Cross-feature domain helpers (shared value objects, failures) — use sparingly.
/// Prefer feature-local domain; only put truly shared concepts here.
abstract final class SharedDomainPlaceholder {
  const SharedDomainPlaceholder._();
}
