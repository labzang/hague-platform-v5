/// Optional global service locator (e.g. get_it / injectable) registration.
/// Prefer Riverpod in features; extend this only when a non-Riverpod API needs it.
abstract final class CoreDiPlaceholder {
  const CoreDiPlaceholder._();
}
